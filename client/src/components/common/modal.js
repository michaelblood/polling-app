import React, { PropTypes } from 'react';

const Modal = props => {
  return (
    <div className="modal">
      <div className="modal-content">
        
      </div>
    </div>
  );
};

Modal.propTypes = {
  
};

export default Modal;